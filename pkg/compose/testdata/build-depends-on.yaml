name: test-build-depends-on
services:
  frontend:
    build:
      context: ./frontend
      depends_on:
        - database
    depends_on:
      - database

  database:
    image: postgres:13
    environment:
      POSTGRES_DB: testdb
      POSTGRES_USER: testuser  
      POSTGRES_PASSWORD: testpass